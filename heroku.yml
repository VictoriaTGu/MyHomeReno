build:
  languages:
    - python
    - nodejs
  buildpacks:
    - heroku/python
    - heroku/nodejs

run:
  web: cd mybackend && gunicorn mybackend.wsgi --log-file -
  release: cd mybackend && python manage.py migrate
